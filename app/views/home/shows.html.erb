<%
  if @current_genre.nil? # 디폴트로 모아보기
    @current_genre = "모아보기"
  end
%>
<div class="containner">
    <nav class="navbar navbar-default" role="navigation" style="margin-top:70px">
    <div class="container-fluid">
      <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
      </div>
      <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
        <ul class="nav navbar-nav">
          <li><a>시간순</a></li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">장르별<span class="caret"></span></a>
            <ul class="dropdown-menu">
                <li><%= link_to "모아보기", g: "모아보기" %></li>
                <li class="divider"></li>
              <% @names.each do |s| %>
                <li><%= link_to "#{s}", controller: "home", action: "shows", g: "#{s}" %></li>
              <% end %>
            </ul>
          </li>
          <li><a style="color:orange; font-weight:bold"><%= @current_genre %></a></li>
          <li><h2 class="navbar-text navbar-right">실시간 공연</h2></li>
        </ul>
      </div>
    </div>
  </nav>
  <div style="margin-top:-20px">
    <%= render partial: "/layouts/show_genre", object: @current_genre %>
  </div>
</div>
