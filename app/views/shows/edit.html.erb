<%= render :partial => "/layouts/headers/header" %>
<%= render :partial => "/layouts/headers/dashboard" %>
<p><%= flash[:alert] %></p>

<!-- 보안문제로 form helper를 사용해서 아래를 수정한뒤, ApplicationController의 protect_from_forgery 주석을 해제해야 한다.-->
<div class="form-group add-show">
  <div class="ui attached message">
    <div class="header">당신의 게릴라트</div>
    <p>아래의 양식을 수정합니다</p>
  </div>

  <div class="ui writeshow form attached fluid segment">
  <%= form_tag (edit_show_path) do %>
    <div class="required field">
      <label>공연 타이틀</label>
      <input type="text" name="show_title" class="form-control" id="1inputName" placeholder="<%= @one_show.title %>" required>
    </div>

    <div class="required field">
      <label>날짜/시간</label>
      <div class="ui calendar" id="date-time">
        <div class="ui input left icon">
          <i class="calendar icon"></i>
        <input type="text" name="time" placeholder="<%= @one_show.time %>" required>
        </div>
      </div>
    </div>

    <div class="required field">
      <label>Play List</label>
      <input type="text" name="playlist"class="form-control" id="6inputName" placeholder="<%= @one_show.playlist %>" required>
    </div>
    <div class="required field">
      <label>공연 소개</label>
      <textarea class="form-control" name="show_content"rows="5" id="comment" placeholder="<%= @one_show.content %>"></textarea>
    </div>

    <div class="inline fields" id="radio-button">
      <label>장르</label>
      <% @names.reverse.each do |n| %>
      <div class="field">
        <div class="ui radio checkbox">
          <% if n == @one_show.genre %>
          <input type="radio" name="genre" checkbox="/" value="<%= n %>" checked="checked">
          <label><%= n %></label>
          <% else %>
          <input type="radio" name="genre" checkbox="/" value="<%= n %>">
          <label><%= n %></label>
          <% end %>
        </div>
      </div>
      <% end %>
    </div>
    <div class="required field">
      <label>장소</label>
      <input type="text" class="form-control" name="location" placeholder="<%= @one_show.location %>" required>
    </div>

    <div class="required field">
      <div class="ui checkbox" >
        <input type="checkbox" tabindex="0" class="hidden">
        <label>I agree to the Terms and Conditions</label>
      </div>
    </div>

    <input type="submit" class="ui fluid blue submit button" id="go-Guerillart" value="등록하기">
  <% end %>
  </div>
</div>
